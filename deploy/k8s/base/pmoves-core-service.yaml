apiVersion: v1
kind: Service
metadata:
  name: pmoves-core
  labels:
    app.kubernetes.io/name: pmoves-core
    app.kubernetes.io/part-of: pmoves-ai
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: pmoves-core
  ports:
    - name: http
      port: 80
      targetPort: http
