diff --git a/README.md b/README.md
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/README.md
@@ 0,0 1,3 @@
+# PMOVES.AI — Orchestration Mesh (Starter)
+
+Quickstart in README; see compose + services.

diff --git a/.env.example b/.env.example
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/.env.example
@@ 0,0 1,18 @@
+TZ=America/New_York
+QDRANT_URL=http://qdrant:6333
+QDRANT_COLLECTION=pmoves_chunks
+NEO4J_URL=bolt://neo4j:7687
+NEO4J_USER=neo4j
+NEO4J_PASSWORD=neo4j
+USE_MEILI=false
+MEILI_MASTER_KEY=master_key
+HIRAG_HTTP_PORT=8086
+GRAPH_BOOST=0.15
+ENTITY_CACHE_TTL=60
+ENTITY_CACHE_MAX=1000
+NEO4J_DICT_REFRESH_SEC=60
+NEO4J_DICT_LIMIT=50000
+TAILSCALE_ONLY=true
+TAILSCALE_ADMIN_ONLY=true
+TAILSCALE_CIDRS=100.64.0.0/10
+EVAL_HTTP_PORT=8090

diff --git a/Makefile b/Makefile
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/Makefile
@@ 0,0 1,10 @@
+.PHONY: up down clean
+up:
+	docker compose --profile data up -d qdrant neo4j minio meilisearch
+	docker compose --profile workers up -d hi-rag-gateway retrieval-eval
+
+down:
+	docker compose down
+
+clean:
+	docker compose down -v

diff --git a/docker-compose.yml b/docker-compose.yml
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/docker-compose.yml
@@ 0,0 1,77 @@
+version: "3.9"
+services:
+  qdrant:
+    image: qdrant/qdrant:v1.10.0
+    restart: unless-stopped
+    ports: ["6333:6333"]
+    profiles: ["data"]
+  meilisearch:
+    image: getmeili/meilisearch:v1.8
+    restart: unless-stopped
+    environment:
+      - MEILI_ENV=production
+      - MEILI_MASTER_KEY=${MEILI_MASTER_KEY:-master_key}
+    ports: ["7700:7700"]
+    profiles: ["data"]
+  neo4j:
+    image: neo4j:5.22
+    restart: unless-stopped
+    environment:
+      - NEO4J_AUTH=${NEO4J_USER}/${NEO4J_PASSWORD}
+    ports: ["7474:7474","7687:7687"]
+    volumes:
+      - neo4j-data:/data
+      - ./neo4j/cypher:/cypher:ro
+    profiles: ["data"]
+  minio:
+    image: minio/minio:RELEASE.2025-02-14T21-00-00Z
+    command: server /data --console-address ":9001"
+    environment:
+      - MINIO_ROOT_USER=minioadmin
+      - MINIO_ROOT_PASSWORD=minioadmin
+    ports: ["9000:9000","9001:9001"]
+    volumes:
+      - minio-data:/data
+    profiles: ["data"]
+  hi-rag-gateway:
+    build: ./services/hi-rag-gateway
+    restart: unless-stopped
+    env_file: [.env]
+    environment:
+      - QDRANT_URL=${QDRANT_URL}
+      - QDRANT_COLLECTION=${QDRANT_COLLECTION}
+      - SENTENCE_MODEL=all-MiniLM-L6-v2
+      - HIRAG_HTTP_PORT=${HIRAG_HTTP_PORT}
+      - NEO4J_URL=${NEO4J_URL}
+      - NEO4J_USER=${NEO4J_USER}
+      - NEO4J_PASSWORD=${NEO4J_PASSWORD}
+      - GRAPH_BOOST=${GRAPH_BOOST}
+      - ENTITY_CACHE_TTL=${ENTITY_CACHE_TTL}
+      - ENTITY_CACHE_MAX=${ENTITY_CACHE_MAX}
+      - USE_MEILI=${USE_MEILI}
+      - MEILI_URL=http://meilisearch:7700
+      - MEILI_API_KEY=${MEILI_MASTER_KEY}
+      - NEO4J_DICT_REFRESH_SEC=${NEO4J_DICT_REFRESH_SEC}
+      - NEO4J_DICT_LIMIT=${NEO4J_DICT_LIMIT}
+      - TAILSCALE_ONLY=${TAILSCALE_ONLY}
+      - TAILSCALE_ADMIN_ONLY=${TAILSCALE_ADMIN_ONLY}
+      - TAILSCALE_CIDRS=${TAILSCALE_CIDRS}
+    ports: ["8086:8086"]
+    profiles: ["workers"]
+    depends_on: [qdrant, neo4j]
+  retrieval-eval:
+    build: ./services/retrieval-eval
+    restart: unless-stopped
+    env_file: [.env]
+    environment:
+      - HIRAG_URL=http://hi-rag-gateway:8086
+      - EVAL_HTTP_PORT=${EVAL_HTTP_PORT}
+    depends_on: [hi-rag-gateway]
+    ports: ["8090:8090"]
+    volumes:
+      - ./services/retrieval-eval:/app:rw
+      - ./datasets:/app/data:rw
+    profiles: ["workers"]
+volumes:
+  neo4j-data: {}
+  minio-data: {}

diff --git a/services/hi-rag-gateway/requirements.txt b/services/hi-rag-gateway/requirements.txt
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/services/hi-rag-gateway/requirements.txt
@@ 0,0 1,2 @@
+fastapi==0.114.2
+uvicorn==0.30.6

diff --git a/services/hi-rag-gateway/Dockerfile b/services/hi-rag-gateway/Dockerfile
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/services/hi-rag-gateway/Dockerfile
@@ 0,0 1,7 @@
+FROM python:3.11-slim
+WORKDIR /app
+COPY requirements.txt ./
+RUN pip install --no-cache-dir -r requirements.txt
+COPY . .
+EXPOSE 8086
+CMD ["python","gateway.py"]

diff --git a/services/hi-rag-gateway/gateway.py b/services/hi-rag-gateway/gateway.py
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/services/hi-rag-gateway/gateway.py
@@ 0,0 1,6 @@
+from fastapi import FastAPI
+app = FastAPI()
+@app.get('/')
+def ok(): return {'ok': True}
+if __name__=='__main__':
+ import uvicorn; uvicorn.run(app, host='0.0.0.0', port=8086)

diff --git a/services/retrieval-eval/requirements.txt b/services/retrieval-eval/requirements.txt
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/services/retrieval-eval/requirements.txt
@@ 0,0 1,2 @@
+fastapi==0.114.2
+uvicorn==0.30.6

diff --git a/services/retrieval-eval/Dockerfile b/services/retrieval-eval/Dockerfile
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/services/retrieval-eval/Dockerfile
@@ 0,0 1,7 @@
+FROM python:3.11-slim
+WORKDIR /app
+COPY requirements.txt ./
+RUN pip install --no-cache-dir -r requirements.txt
+COPY . .
+EXPOSE 8090
+CMD ["python","server.py"]

diff --git a/services/retrieval-eval/server.py b/services/retrieval-eval/server.py
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/services/retrieval-eval/server.py
@@ 0,0 1,6 @@
+from fastapi import FastAPI
+app=FastAPI()
+@app.get('/')
+def idx(): return {'ok':True}
+if __name__=='__main__':
+ import uvicorn; uvicorn.run(app, host='0.0.0.0', port=8090)

diff --git a/services/retrieval-eval/static/index.html b/services/retrieval-eval/static/index.html
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/services/retrieval-eval/static/index.html
@@ 0,0 1,1 @@
+<html><body>Eval Dashboard Placeholder</body></html>

diff --git a/services/agents/pubsub_stub.py b/services/agents/pubsub_stub.py
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/services/agents/pubsub_stub.py
@@ 0,0 1,1 @@
+def publish(t,p): print(t,p)

diff --git a/datasets/pmoves_smoke.json b/datasets/pmoves_smoke.json
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/datasets/pmoves_smoke.json
@@ 0,0 1,4 @@
+{
+  "id": "pmoves_smoke",
+  "queries": []
+}

diff --git a/schemas/ingest.document.v1.json b/schemas/ingest.document.v1.json
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/schemas/ingest.document.v1.json
@@ 0,0 1,5 @@
+{
+  "$schema": "https://json-schema.org/draft/2020-12/schema",
+  "title": "ingest.document.v1.json",
+  "type": "object"
+}

diff --git a/schemas/analysis.entities.v1.json b/schemas/analysis.entities.v1.json
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/schemas/analysis.entities.v1.json
@@ 0,0 1,5 @@
+{
+  "$schema": "https://json-schema.org/draft/2020-12/schema",
+  "title": "analysis.entities.v1.json",
+  "type": "object"
+}

diff --git a/schemas/gen.asset.v1.json b/schemas/gen.asset.v1.json
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/schemas/gen.asset.v1.json
@@ 0,0 1,5 @@
+{
+  "$schema": "https://json-schema.org/draft/2020-12/schema",
+  "title": "gen.asset.v1.json",
+  "type": "object"
+}

diff --git a/schemas/kb.write.v1.json b/schemas/kb.write.v1.json
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/schemas/kb.write.v1.json
@@ 0,0 1,5 @@
+{
+  "$schema": "https://json-schema.org/draft/2020-12/schema",
+  "title": "kb.write.v1.json",
+  "type": "object"
+}

diff --git a/supabase/sql/001_init.sql b/supabase/sql/001_init.sql
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/supabase/sql/001_init.sql
@@ 0,0 1,3 @@
+create table if not exists agent_memory(id bigserial primary key);
+create table if not exists extractions(id bigserial primary key);
+create table if not exists studio_board(id bigserial primary key);

diff --git a/n8n/flows/echo_publisher.json b/n8n/flows/echo_publisher.json
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/n8n/flows/echo_publisher.json
@@ 0,0 1,3 @@
+{
+  "name": "Echo Publisher"
+}

diff --git a/n8n/flows/approval_poller.json b/n8n/flows/approval_poller.json
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/n8n/flows/approval_poller.json
@@ 0,0 1,3 @@
+{
+  "name": "Supabase Approval Poller"
+}

diff --git a/neo4j/cypher/001_init.cypher b/neo4j/cypher/001_init.cypher
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/neo4j/cypher/001_init.cypher
@@ 0,0 1,1 @@
+CREATE CONSTRAINT IF NOT EXISTS ON (e:Entity) ASSERT e.value IS UNIQUE;

diff --git a/comfyui/minio_loader.py b/comfyui/minio_loader.py
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/comfyui/minio_loader.py
@@ 0,0 1,1 @@
+def presign(bucket,key): return f'https://example/{bucket}/{key}'

diff --git a/.github/workflows/ci.yml b/.github/workflows/ci.yml
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/.github/workflows/ci.yml
@@ 0,0 1,3 @@
+name: pmoves-ci
+on: {push: {branches: [main, dev]}, pull_request: {}}
+jobs: {noop: {runs-on: ubuntu-latest, steps: [{uses: actions/checkout@v4}]}}
