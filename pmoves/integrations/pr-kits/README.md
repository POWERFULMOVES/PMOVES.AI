# PMOVES PR Kits

`pr-kits/` stores reusable PR packaging assets (compose snippets, CI fragments, summary docs).

This directory is **not** a runtime integration overlay and is intentionally excluded from strict
`pmoves-integrations` contract checks (`compose/models/events/secrets/auth/tools/docs` scaffold).

Use this folder for onboarding/merge collateral only.
