{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "DeepResearchRequest",
  "type": "object",
  "required": ["prompt"],
  "properties": {
    "prompt": {
      "type": "string",
      "description": "Primary instruction or question for the deep research agent."
    },
    "context": {
      "type": "array",
      "description": "Additional free-form context strings provided by the caller.",
      "items": {
        "type": "string"
      }
    },
    "metadata": {
      "type": "object",
      "description": "Arbitrary metadata to forward to the runner.",
      "additionalProperties": true
    },
    "notebook": {
      "type": "object",
      "description": "Notebook payload that should accompany the research run.",
      "additionalProperties": true
    },
    "model": {
      "type": ["string", "null"],
      "description": "Optional model hint (e.g. openrouter identifier)."
    },
    "resources": {
      "type": "object",
      "description": "External resources the runner can enrich the context with.",
      "properties": {
        "cookbooks": {
          "type": "array",
          "description": "List of Qwen cookbook paths or raw URLs to merge into context.",
          "items": {
            "type": "string"
          }
        }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": true
  "title": "DeepResearch Request v1",
  "type": "object",
  "required": ["query"],
  "additionalProperties": false,
  "properties": {
    "query": {
      "type": "string",
      "minLength": 1,
      "description": "Natural language research question or task description."
    },
    "mode": {
      "type": "string",
      "description": "Execution mode override (e.g., openrouter, local)."
    },
    "max_steps": {
      "type": "integer",
      "minimum": 1,
      "description": "Optional cap on iterative reasoning/tool steps."
    },
    "context": {
      "type": "object",
      "description": "Structured context or hints forwarded to the research agent.",
      "additionalProperties": true
    },
    "metadata": {
      "type": "object",
      "description": "Opaque metadata echoed back with the result payload.",
      "additionalProperties": true
    },
    "notebook": {
      "type": "object",
      "description": "Open Notebook integration overrides.",
      "additionalProperties": false,
      "properties": {
        "notebook_id": {
          "type": "string",
          "minLength": 1,
          "description": "Notebook UUID to receive the research summary."
        },
        "title_prefix": {
          "type": "string",
          "description": "Optional prefix appended to the Notebook source title."
        },
        "embed": {
          "type": "boolean",
          "description": "Whether Notebook should embed the content during ingestion."
        },
        "async_processing": {
          "type": "boolean",
          "description": "If true, Notebook may process the source asynchronously."
        }
      }
    }
  }
}
