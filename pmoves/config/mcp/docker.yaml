id: docker
name: Docker MCP Toolkit
required_commands:
  - docker
setup_instructions:
  - Install Docker Engine and ensure the daemon is running.
  - Install the Docker MCP toolkit (pip install mcp-docker) or the official binary package.
  - Grant the pmoves mini CLI permission to access the Docker socket.
health_check:
  command: ["docker", "ps"]
