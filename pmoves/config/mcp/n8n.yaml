id: n8n
name: n8n MCP Bridge
required_commands:
  - curl
setup_instructions:
  - Ensure n8n container is running (`make integrations-up-core` or profile bring-up).
  - Configure API credentials in ~/.pmoves/mcp/n8n.yaml (base URL, API key).
  - Install the pmoves n8n MCP adaptor (todo).
health_check:
  command: ["curl", "-sf", "http://localhost:5678/healthz"]
