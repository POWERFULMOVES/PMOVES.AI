# MCP profile: pmoves_4090_web
# Node: laptop-4090 (coding workstation, RTX 4090 laptop, 14.3 GB usable VRAM)
#
# Consumed by: .claude/mcp.json (pmoves-4090-web SSE entry)
# Profile def:  pmoves/docker/pmoves-4090-web/profile.yaml
# Make targets: pmoves/mk/mcp-toolkit-4090.mk
#
# Prerequisites:
#   1. `make mcp-4090-gateway-start`  — starts SSE gateway at localhost:8090
#   2. MCP_GATEWAY_AUTH_TOKEN exported (auto-generated by gateway script, persisted to env.shared)
#
# Lane A (docker mcp client connect claude-code --profile pmoves_4090_web) is deferred
# pending Docker MCP auto-mode classifier auth resolution — same blocker as pmoves_5090_web.

name: pmoves-4090-web
transport: sse
url: "http://localhost:8090/sse"
auth_header: "Authorization: Bearer ${MCP_GATEWAY_AUTH_TOKEN}"
profile: pmoves_4090_web
node: laptop-4090
