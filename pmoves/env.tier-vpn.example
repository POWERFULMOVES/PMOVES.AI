# =============================================================================
# PMOVES Remote Desktop & VPN Tier Environment Variables
# =============================================================================
# Services: headscale, rustdesk-hbbs, rustdesk-hbbr, headscale-agent,
#           vpn-mcp (via BoTZ)
# =============================================================================

# Remote desktop & VPN tier provides self-hosted remote access
# Headscale: Self-hosted Tailscale control server (WireGuard VPN mesh)
# RustDesk: Self-hosted remote desktop relay server

# ---------------------------------------------------------------------------
# Headscale Control Server
# ---------------------------------------------------------------------------
HEADSCALE_SERVER_URL=https://headscale.pmoves.local
HEADSCALE_PORT=8096
# Generate with: openssl rand -hex 32
HEADSCALE_API_KEY=change-me-generated-secret
HEADSCALE_ADDR=headscale:8096

# ---------------------------------------------------------------------------
# Headscale Agent (VPN client for PMOVES server itself)
# ---------------------------------------------------------------------------
# Get auth key from: docker exec pmoves-headscale headscale apikeys create
HEADSCALE_AGENT_AUTH_KEY=
HEADSCALE_TAGS=tag:pmoves-server,tag:infra

# ---------------------------------------------------------------------------
# RustDesk Configuration
# ---------------------------------------------------------------------------
RUSTDESK_IMAGE=rustdesk/rustdesk-server:latest
RUSTDESK_RELAY=pmoves-rustdesk-hbbr:21117
RUSTDESK_ENCRYPTED_ONLY=0

# ---------------------------------------------------------------------------
# BoTZ VPN MCP Integration
# ---------------------------------------------------------------------------
VPN_MANAGER_URL=http://vpn-mcp:8110/sse

# ---------------------------------------------------------------------------
# Remote Desktop Feature Flags
# ---------------------------------------------------------------------------
REMOTE_DESKTOP_ENABLED=false
REMOTE_DESKTOP_ADMIN_ONLY=true
