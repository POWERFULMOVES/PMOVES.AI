## Local/Remote Hybrid mode (Supabase hosted on VPS)
# Copy to .env.hybrid (or merge into .env.local) when you want the local stack
# to talk to your remote Supabase deployment. Shared secrets live in env.shared.

# Tell Make/Compose not to start the local Supabase services.
SUPABASE_RUNTIME=remote
SUPA_PROVIDER=remote
EXTERNAL_SUPABASE=true

# Host bindings for the remote stack (usually no changes needed)
SUPA_REST_URL=${SUPABASE_URL}/rest/v1
SUPA_REST_INTERNAL_URL=${SUPABASE_URL}/rest/v1
SUPABASE_REALTIME_URL=${SUPABASE_URL}/realtime/v1
GOTRUE_SITE_URL=${SUPABASE_URL}/auth/v1
SUPABASE_STORAGE_URL=${SUPABASE_URL}/storage/v1
SUPABASE_PUBLIC_STORAGE_BASE=${SUPABASE_URL}/storage/v1

# Optional overrides for tailscale/lan routing
# SUPABASE_URL=https://supabasepmoves.cataclysmstudios.net
# SUPABASE_INTERNAL_URL=http://100.x.y.z:65421
