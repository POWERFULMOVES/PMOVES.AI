## Local overrides for Supabase CLI mode (Option A)
# Copy this to .env.local for local development against Supabase CLI.
# Values here override .env for services that include env_file: [.env, .env.local].

# Supabase (CLI defaults; confirm via `make supa-status`)
SUPA_REST_URL=http://localhost:65421/rest/v1
GOTRUE_SITE_URL=http://localhost:54323
SUPABASE_STORAGE_URL=http://localhost:5000
SUPABASE_PUBLIC_STORAGE_BASE=http://localhost:5000

# Next.js UI workspace expects these Supabase endpoints as well
SUPABASE_URL=http://127.0.0.1:65421
SUPABASE_REST_URL=http://127.0.0.1:65421/rest/v1
SUPABASE_REALTIME_URL=ws://127.0.0.1:65421/realtime/v1
PMOVES_API_URL=http://localhost:8080
PMOVES_WS_URL=ws://localhost:8080

# Keys (populate from `make supa-status` output)
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_JWT_SECRET=
NEXT_PUBLIC_SUPABASE_URL=http://localhost:65421
NEXT_PUBLIC_SUPABASE_ANON_KEY=
NEXT_PUBLIC_SUPABASE_AUTH_CALLBACK_URL=http://localhost:3000/callback
NEXT_PUBLIC_SUPABASE_PASSWORD_AUTH_ENABLED=true
NEXT_PUBLIC_SUPABASE_OAUTH_ENABLED=true

# Mode toggles (leave default unless you switch to hybrid)
SUPABASE_RUNTIME=cli
SUPA_PROVIDER=cli

# Optional overrides (uncomment as needed)
# OPEN_NOTEBOOK_UI_PORT=8503
# OPEN_NOTEBOOK_API_PORT=5055
