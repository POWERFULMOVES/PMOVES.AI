# Security: Set TORCH_FORCE_WEIGHTS_ONLY_LOAD=1 for safe model loading (weights_only=True)
# CPU fallback

fastapi==0.114.2
uvicorn[standard]==0.30.6
# Object detection is engine-selectable (DETECTION_ENGINE):
#  - detr (default): facebook/detr-resnet-50, Apache-2.0, via transformers+timm.
#  - yolo: Ultralytics YOLO. AGPL-3.0 — fine to RUN on a private/self-hosted fleet
#    (ideal on Jetson); the ship rule governs distribution, not private use. Kept
#    installed so DETECTION_ENGINE=yolo works out of the box; operator's call.
ultralytics==8.2.103
opencv-python-headless>=4.10.0
Pillow==12.3.0
boto3==1.34.162
requests==2.33.0
nats-py==2.7.2
supabase==2.3.1
transformers>=5.0.0rc3
timm==0.9.16
accelerate==0.32.1
sentencepiece==0.2.1
einops==0.8.0
prometheus-client>=0.19.0
