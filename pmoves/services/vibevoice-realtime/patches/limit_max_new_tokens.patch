diff --git a/demo/web/app.py b/demo/web/app.py
--- a/demo/web/app.py
+++ b/demo/web/app.py
@@ -209,9 +209,17 @@ class StreamingTTSService:
         stop_event: threading.Event,
     ) -> None:
         try:
+            max_new_tokens_env = os.environ.get("VIBEVOICE_MAX_NEW_TOKENS", "512")
+            try:
+                max_new_tokens = int(max_new_tokens_env)
+            except Exception:
+                max_new_tokens = 512
+            if max_new_tokens <= 0:
+                max_new_tokens = None
+
             self.model.generate(
                 **inputs,
-                max_new_tokens=None,
+                max_new_tokens=max_new_tokens,
                 cfg_scale=cfg_scale,
                 tokenizer=self.processor.tokenizer,
                 generation_config={
