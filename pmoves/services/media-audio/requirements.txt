--extra-index-url https://download.pytorch.org/whl/cu124
# Media Audio Analyzer - Audio processing with Whisper and emotion detection
# Security: Set TORCH_FORCE_WEIGHTS_ONLY_LOAD=1 for safe model loading
# GPU-enabled: CUDA 12.4 runtime with PyTorch support

# Core ML framework
torch==2.6.0 ; platform_machine == "x86_64"
torchvision==0.21.0+cu124 ; platform_machine == "x86_64"
torchaudio==2.6.0+cu124 ; platform_machine == "x86_64"
# CPU fallback
torch==2.6.0 ; platform_machine != "x86_64"
torchvision==0.21.0 ; platform_machine != "x86_64"
torchaudio==2.6.0 ; platform_machine != "x86_64"

# Audio processing
openai-whisper>=20240930
librosa>=0.10.0
soundfile>=0.12.0
pydub>=0.25.1
# Speaker diarization — loaded only when HF_TOKEN is set (pyannote 3.1 is gated).
pyannote.audio>=3.3.0

# API framework
fastapi==0.114.2
uvicorn[standard]==0.30.6

# PMOVES integration
nats-py==2.7.2
supabase==2.3.1
boto3==1.34.162
requests==2.33.0

# ML utilities
transformers>=5.0.0rc3
accelerate==0.32.1

# Data validation
pydantic>=2.0.0
jsonschema>=4.0.0
prometheus-client>=0.19.0
