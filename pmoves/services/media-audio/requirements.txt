# Media Audio Analyzer - Audio processing with Whisper and emotion detection
# Security: Set TORCH_FORCE_WEIGHTS_ONLY_LOAD=1 for safe model loading
# GPU-enabled: CUDA 12.4 runtime with PyTorch support

# Core ML framework
# CPU fallback

# Audio processing
openai-whisper>=20240930
numba>=0.59.0  # Python 3.11 compat (old numba fails on 3.11+)
librosa>=0.10.0
soundfile>=0.12.0
pydub>=0.25.1
# Speaker diarization — loaded only when HF_TOKEN is set (pyannote 3.1 is gated).
# <4 is load-bearing: pyannote 4.x hard-requires torchcodec>=0.7, which aborts
# on import (std::length_error) in this image — reproduced as a 202-restart
# crash loop. The 3.4 stack is the documented, working path.
pyannote.audio>=3.3.0,<4

# API framework
fastapi==0.114.2
uvicorn[standard]==0.30.6

# PMOVES integration
nats-py==2.7.2
supabase==2.3.1
boto3==1.34.162
requests==2.33.0

# ML utilities
# <5 is load-bearing: transformers 5 forces huggingface-hub>=1.0, which breaks
# pyannote 3.x (use_auth_token removed); pyannote 4.x in turn hard-requires
# torchcodec>=0.7, which aborts on import (std::length_error) in this image.
# whisper-large-v3-turbo + hubert emotion are 4.x-era architectures.
transformers>=4.53,<5
accelerate>=1.0  # pyannote.audio 4.x + transformers 5.x need modern accelerate (was ==0.32.1)

# Data validation
pydantic>=2.0.0
jsonschema>=4.0.0
prometheus-client>=0.19.0
