# ActivePieces Git Sync target

Flows-as-code land here when Git Sync is configured (AP_EDITION=ee) on both the
hosted (cataclysmstudios@gmail.com) project and this self-host, pointed at this
directory. Mirrors pmoves/n8n/flows/. On Community edition, export flows from the
cloud and commit their JSON here (see ../README.md).
