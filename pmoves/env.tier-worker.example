# =============================================================================
# PMOVES Worker Tier Environment Variables
# Services: extract-worker, pdf-ingest, langextract, notebook-sync,
#           session-context-worker, render-webhook, comfy-watcher
# =============================================================================

# Worker tier services connect to internal gateways (not external APIs)
# They should NEVER have direct access to LLM provider API keys

# ---------------------------------------------------------------------------
# MinIO Object Storage (internal)
# ---------------------------------------------------------------------------
MINIO_ENDPOINT=minio:9000
MINIO_ACCESS_KEY=  # REQUIRED: Set from env.tier-data
MINIO_SECRET_KEY=  # REQUIRED: Set from env.tier-data
MINIO_SECURE=false

# ---------------------------------------------------------------------------
# NATS Message Bus (internal)
# ---------------------------------------------------------------------------
NATS_URL=nats://nats:4222

# ---------------------------------------------------------------------------
# TensorZero Gateway (internal - workers route through this)
# ---------------------------------------------------------------------------
TENSORZERO_BASE_URL=http://tensorzero-gateway:3000

# ---------------------------------------------------------------------------
# Extract Worker (internal)
# ---------------------------------------------------------------------------
EXTRACT_WORKER_URL=http://extract-worker:8083

# ---------------------------------------------------------------------------
# LangExtract Settings
# ---------------------------------------------------------------------------
LANGEXTRACT_PROVIDER=tensorzero
LANGEXTRACT_URL=http://langextract:8084

# ---------------------------------------------------------------------------
# Open Notebook (external - via host gateway)
# ---------------------------------------------------------------------------
OPEN_NOTEBOOK_API_URL=http://host.docker.internal:5055
OPEN_NOTEBOOK_API_TOKEN=

# ---------------------------------------------------------------------------
# Render Webhook
# ---------------------------------------------------------------------------
RENDER_WEBHOOK_SHARED_SECRET=  # REQUIRED: Generate with `openssl rand -hex 32`
RENDER_AUTO_APPROVE=false

# ---------------------------------------------------------------------------
# Supabase/PostgREST (internal)
# ---------------------------------------------------------------------------
SUPA_REST_URL=http://postgrest:3000
