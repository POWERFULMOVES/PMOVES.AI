diff --git a/docker-compose.yml b/docker-compose.yml
index 8a3a1d1..d4f4f77 100644
--- a/docker-compose.yml
+++ b/docker-compose.yml
@@ -20,6 +20,7 @@ services:
   qdrant:
     image: qdrant/qdrant:latest
     restart: unless-stopped
+    profiles: ["qdrant-local"]
     ports: ["6333:6333"]
     volumes:
       - qdrant_data:/qdrant/storage
@@ -32,6 +33,7 @@ services:
   meilisearch:
     image: getmeili/meilisearch:latest
     restart: unless-stopped
+    profiles: ["meili-local"]
     environment:
       - MEILI_ENV=production
       - MEILI_MASTER_KEY=${MEILI_MASTER_KEY:-master_key}
@@ -44,6 +46,7 @@ services:
   neo4j:
     image: neo4j:5
     restart: unless-stopped
+    profiles: ["neo4j-local"]
     ports: ["7474:7474","7687:7687"]
     environment:
       - NEO4J_AUTH=${NEO4J_USER}/${NEO4J_PASSWORD}
@@ -58,6 +61,7 @@ services:
   postgrest:
     image: postgrest/postgrest:latest
     restart: unless-stopped
+    profiles: ["supabase-local"]
     environment:
       - PGRST_DB_URI=${DATABASE_URL}
       - PGRST_DB_SCHEMAS=public
@@ -70,6 +74,7 @@ services:
   postgres:
     image: postgres:15
     restart: unless-stopped
+    profiles: ["supabase-local"]
     environment:
       - POSTGRES_USER=${POSTGRES_USER}
       - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
@@ -140,6 +145,7 @@ services:
   hi-rag-gateway-v2:
     build: ./services/hi-rag-gateway
     restart: unless-stopped
+    profiles: ["gateway"]
     env_file: [.env]
     # … existing env/ports …
 
@@ -220,6 +226,7 @@ services:
   media-video:
     build: ./services/media-video
     restart: unless-stopped
+    profiles: ["workers"]
     env_file: [.env]
     # GPU overrides provided in docker-compose.gpu.yml
