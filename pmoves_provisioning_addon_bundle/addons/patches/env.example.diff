diff --git a/.env.example b/.env.example
index 7c1b2ee..8f5a97a 100644
--- a/.env.example
+++ b/.env.example
@@ -50,3 +50,15 @@ MEILI_MASTER_KEY=master_key
 QDRANT_URL=http://qdrant:6333
 QDRANT_COLLECTION=pmoves_kb
 
+# ---- External-mode flags (skip local service; point to your own endpoints) ----
+# When true, the local container for the service is NOT started. Make sure the
+# corresponding *_URL / keys above point to your external instance.
+EXTERNAL_NEO4J=false
+EXTERNAL_MEILI=false
+EXTERNAL_QDRANT=false
+EXTERNAL_SUPABASE=false
+
+# MinIO convenience (for backups)
+MINIO_BUCKET=assets
+# Meili dump requires the admin key defined above
+# POSTGRES_USER/POSTGRES_DB already defined in existing section
